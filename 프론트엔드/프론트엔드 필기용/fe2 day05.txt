아이콘에다 width + transition을 적용해서 아이콘을 변환시킬수 있음

마커마다 일일이 trantlate 위치를 화면 크기마다 다르게 해서 수동적으로라도 마커를 위치 변화 시킬 수 있음

canvas 태그: 그림을 그릴 때 사용

grid를 이용한 사진 분배

grid 안에 grid 속성을 추가하여 이중으로 배치를 할 수 있음


tailwind가 아닌 다른 CSS

youtube는 자체적으로 ytd를 사용


class 이름

유튜브

app
id와 class의 동시사용

id를 꽤 많이 활용함
content 안의 content

overflow시 scroll 처리

byline-seperate 작성자의 이름

badge -> 작은 이미지, 알림...

iframe : html에서 다른 html을 불러 올 때 사용

Hero image

테이블에도 footer 태그를 활용할 수 있음 <tfoot>


Breadcrumb UX
사이트에서 유저의 위치, 이동할 수 있는 사이트 경로 등을 보여주는 네비게이션 역할





